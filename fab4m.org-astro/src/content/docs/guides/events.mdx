---
title: Form events
description: Use event listeners for data changes, submits, and part validation
sidebar:
  order: 4
---

import CodeExample from "../../../components/CodeExample.astro";
import { OnChangeExample } from "../../../components/examples/events/FormChange.tsx";
import OnChangeExampleSource from "../../../components/examples/events/FormChange.tsx?raw";
import { OnComponentChangeExample } from "../../../components/examples/events/ComponentChange.tsx";
import OnComponentChangeSource from "../../../components/examples/events/ComponentChange.tsx?raw";
import { OnSubmitExample } from "../../../components/examples/events/SubmitEvent.tsx";
import SubmitExampleSource from "../../../components/examples/events/SubmitEvent.tsx?raw";
import { OnPartValidateForm } from "../../../components/examples/events/PartValidator.tsx";
import OnPartValidateSource from "../../../components/examples/events/PartValidator.tsx?raw";
import OnChangeExampleSvelteSource from "../../../components/examples/events/FormChange.svelte?raw";
import OnComponentChangeSvelteSource from "../../../components/examples/events/ComponentChange.svelte?raw";
import SubmitExampleSvelteSource from "../../../components/examples/events/SubmitEvent.svelte?raw";
import OnPartValidateSvelteSource from "../../../components/examples/events/PartValidator.svelte?raw";

# Form events

Each form created by fab4m provides several events that can be used
to be notified about the form lifecycle. We provide events for when data changes,
when data is submitted, and when data is being validated.

## Listen for all data changes

Each time any component in the form changes the `onDataChange` event is fired.
All form data is passed as an object.

<CodeExample reactCode={OnChangeExampleSource} svelteCode={OnChangeExampleSvelteSource}>
  <OnChangeExample client:load />
</CodeExample>

## Listen for changes in one specific component

You might not want to handle the whole state, and instead only
listen for changes in certain components. This can be done
using the `onComponentChange` event.

<CodeExample reactCode={OnComponentChangeSource} svelteCode={OnComponentChangeSvelteSource}>
  <OnComponentChangeExample client:load />
</CodeExample>

## Handling form submits

Use the `onSubmit` function to handle form submissions. This function is called
whenever the form is submitted and it's only called if the form data is valid,
so you can rely on your data being complete at this point.

<CodeExample reactCode={SubmitExampleSource} svelteCode={SubmitExampleSvelteSource}>
  <OnSubmitExample client:load />
</CodeExample>

## Using events for custom validation

Sometimes you need to add custom validation for your form. You can use the `onPartValidate` event for this.
The `onPartValidate` function is called each time a form part is submitted.

The listener is asynchronous, so any required server validation
can occur within.

Errors encountered should be returned as an array with the following structure:

```jsx
[{
  path: "/component", // This is a JSON pointer
  message: "This is the message that will be shown in the form"
}];
```

See the validator documentation for more information about validating your data.

:::tip

You can also define custom component validators. This works well when you only need
the data from one specific component, but if you require the data from the full form
you're probably better off using this event instead.

:::

The example below performs validation on each form part:

- On part 1, we ensure the provided age is over 18 and the name doesn't contain "sam"
- On part 2, we encourage the user to switch profession if the name contains "Fabian" and the bio contains "Computer science"

<CodeExample reactCode={OnPartValidateSource} svelteCode={OnPartValidateSvelteSource}>
  <OnPartValidateForm client:load />
</CodeExample>

## Adding multiple form listeners to the same event

Fab4m forms support multiple event listeners for the same event. By default, we override
registered events when adding listeners, because these are usually added inside React components.

If you want to use multiple listeners, append the event handler:

```jsx
form
  .onChange(() => {
    // Listener one.
  }, true)
  .onChange(() => {
    // Listener two.
  }, true);
```
