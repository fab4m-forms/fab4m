---
title: Rendering forms
description: Learn how to render forms with FormView and StatefulFormView
sidebar:
  order: 2
---

import CodeExample from "../../../components/CodeExample.astro";
import BasicExample from "../../../components/examples/rendering/Basic.tsx";
import BasicSource from "../../../components/examples/rendering/Basic.tsx?raw";
import FormStateExample from "../../../components/examples/rendering/FormState.tsx";
import FormStateSource from "../../../components/examples/rendering/FormState.tsx?raw";
import StatefulViewExample from "../../../components/examples/rendering/StatefulView.tsx";
import StatefulViewSource from "../../../components/examples/rendering/StatefulView.tsx?raw";
import ReactHookExample from "../../../components/examples/rendering/ReactHook.tsx";
import ReactHookSource from "../../../components/examples/rendering/ReactHook.tsx?raw";
import BasicSvelteSource from "../../../components/examples/rendering/Basic.svelte?raw";
import FormStateSvelteSource from "../../../components/examples/rendering/FormState.svelte?raw";
import StatefulViewSvelteSource from "../../../components/examples/rendering/StatefulView.svelte?raw";
import ReactHookSvelteSource from "../../../components/examples/rendering/ReactHook.svelte?raw";

Forms are rendered using derivatives of the `FormView` component. This section describes how to work with that component and `StatefulFormView`.

## Basic rendering

The most common way to render a form is through the `<FormView>` component.

<CodeExample reactCode={BasicSource} svelteCode={BasicSvelteSource}>
  <BasicExample client:load />
</CodeExample>

## Managing the form state

The `FormView` component only renders the form with the provided data. It does not manage form state.

<CodeExample reactCode={FormStateSource} svelteCode={FormStateSvelteSource}>
  <FormStateExample client:load />
</CodeExample>

## Handling state automatically

If you don't want to manage the state yourself, use `<StatefulFormView>`.

<CodeExample reactCode={StatefulViewSource} svelteCode={StatefulViewSvelteSource}>
  <StatefulViewExample client:load />
</CodeExample>

## Defining forms inside components

Creating a form definition is resource heavy. In React, use the `useForm()` hook so the form is memoized across renders.

<CodeExample reactCode={ReactHookSource} svelteCode={ReactHookSvelteSource}>
  <ReactHookExample client:load />
</CodeExample>

The `useForm` hook optionally takes a dependency array. Internally it's a wrapper around [useMemo](https://reactjs.org/docs/hooks-reference.html#usememo).

In Svelte, the component script only runs once per instance, so you can create the form directly in the `<script>` block without any special hook. If you want the form to be shared across instances, move it into a `<script module>` block.
