---
description: Run multiple scouts in parallel to investigate different aspects of the codebase
---
Use the subagent tool with the tasks parameter to run parallel investigations:

$@

For each distinct area of investigation, create a separate scout task in the tasks array. This allows multiple scouts to explore different parts of the codebase simultaneously.
