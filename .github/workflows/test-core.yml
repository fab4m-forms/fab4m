name: Run tests
run-name: ${{ github.actor }} test
on: [push]
jobs:
  test:
    strategy:
      matrix:
        node-version: [18.x]
        package: ["core", "password", "date", "routerforms", "autocomplete"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - run: npm ci
      - run: cd packages/core && npm run build
      - name: Check prettier standards
        run:  cd packages/${{matrix.package}} && npx prettier --check src test
      - name: Check eslint rules
        run:  cd packages/${{matrix.package}} && npx lint . --ext .js,.jsx,.ts,.tsx
      - name: Run tests
      - run: cd packages/${{matrix.package}} && npm run test:unit
